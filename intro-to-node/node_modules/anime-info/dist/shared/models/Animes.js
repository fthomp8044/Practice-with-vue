"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g;
    return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (_) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
Object.defineProperty(exports, "__esModule", { value: true });
var jsdom_1 = require("jsdom");
var elements_dom_1 = require("../utils/elements.dom");
var AnimesGeneric = /** @class */ (function () {
    function AnimesGeneric(_lang, _tags, _baseUrl, _defaultOption, _all_characters, _character, _document) {
        if (_character === void 0) { _character = ''; }
        if (_document === void 0) { _document = new jsdom_1.JSDOM().window.document; }
        this._lang = _lang;
        this._tags = _tags;
        this._baseUrl = _baseUrl;
        this._defaultOption = _defaultOption;
        this._all_characters = _all_characters;
        this._character = _character;
        this._document = _document;
    }
    AnimesGeneric.prototype.getListCharacters = function (search) {
        if (search === void 0) { search = ''; }
        var regExp = new RegExp(search, 'i');
        return this._all_characters.filter(function (value) {
            var name = value;
            return regExp.test(name);
        });
    };
    AnimesGeneric.prototype.getCharacter = function (name, option) {
        if (option === void 0) { option = {}; }
        return __awaiter(this, void 0, void 0, function () {
            var _a, keys, _b, only_default, _c, exclude_1, _d, only, new_only, error_1;
            return __generator(this, function (_e) {
                switch (_e.label) {
                    case 0:
                        _e.trys.push([0, 2, , 3]);
                        this._character = name;
                        _a = this;
                        return [4 /*yield*/, elements_dom_1.getDom(this._baseUrl + this._character)];
                    case 1:
                        _a._document = _e.sent();
                        keys = [];
                        _b = this._defaultOption.only, only_default = _b === void 0 ? [] : _b;
                        _c = option.exclude, exclude_1 = _c === void 0 ? [] : _c, _d = option.only, only = _d === void 0 ? [] : _d;
                        if (only.length > 0) {
                            keys.push.apply(keys, only);
                        }
                        else if (exclude_1.length > 0) {
                            new_only = only_default.filter(function (value) { return exclude_1.indexOf(value) < 0; });
                            keys.push.apply(keys, new_only);
                        }
                        else {
                            keys.push.apply(keys, only_default);
                        }
                        return [2 /*return*/, this._toJson(keys)];
                    case 2:
                        error_1 = _e.sent();
                        throw new Error('The requested Character does not exist');
                    case 3: return [2 /*return*/];
                }
            });
        });
    };
    return AnimesGeneric;
}());
exports.AnimesGeneric = AnimesGeneric;
