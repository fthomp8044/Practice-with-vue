"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var elements_dom_1 = require("../../shared/utils/elements.dom");
function getTransformations(document, tag, lang) {
    var list = [];
    var elements = document.querySelectorAll(tag);
    elements.forEach(function (element) {
        if (lang === 'es') {
            list.push(elements_dom_1.cleanString(elements_dom_1.getInnerHTML(element)));
        }
        else {
            var icon = 'Sem Imagem';
            if (element.children[0] && element.children[0].children[0]) {
                icon = element.children[0].children[0].getAttribute('data-src');
            }
            list.push({
                icon: icon,
                name: elements_dom_1.cleanString(elements_dom_1.getInnerHTML(element))
            });
        }
    });
    return list;
}
exports.getTransformations = getTransformations;
